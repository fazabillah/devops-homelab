apiVersion: v1
kind: Service
metadata:
  name: java-screening-app
  labels:
    app: java-screening-app
spec:
  selector:
    app: java-screening-app
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP